// (Optional) space for future interactivity; keeping minimal for now.
document.addEventListener("DOMContentLoaded", () => {
  // Focus the search box on catalog page for convenience
  const q = document.querySelector('.filters input[name="q"]');
  if (q) q.focus();
});
